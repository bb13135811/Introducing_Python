命令	            用途
keys *	        查看所有key的內容
exists key	    查看 key 是否存在
set key value	設置key的內容
get key	        獲取key的內容
flushall	    清空所有


Redis TTL命令用於獲取鍵到期的剩餘時間(秒)
返回值
以毫秒為單位的整數值TTL或負值
TTL以毫秒為單位。
-1, 如果key有到期超時。
-2, 如果鍵不存在。